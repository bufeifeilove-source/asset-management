services:
  memory-web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - memory_queue:/root/.local/share/memory-system
    restart: unless-stopped

volumes:
  memory_queue:
